\import std/nat

\universe U

\record Pair (A : U) (B : U) : U
  | fst : A
  | snd : B

\let swap (A : U) (B : U) : Pair A B -> Pair B A \where
  <= \intro
  <= \split
  | swap A B { fst = a, snd = b, z = c } => { fst = b, snd = a }
