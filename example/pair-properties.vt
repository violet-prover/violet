import pair
import equality

let fst (a b : Nat) : Nat := a
let eq-fst : Eq (fst zero (suc zero)) zero := refl

let zz : Pair Nat Nat := pair zero zero
let eq-p : Eq zz zz := refl
let eq-p1 : Eq zz (pair zero zero) := refl
