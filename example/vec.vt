import nat

data Vec (A : U) : Nat -> U
  | nil : {A : U} -> Vec A zero
  | cons : {A : U} -> {n : Nat} -> A -> Vec A n -> Vec A (suc n)

let n : Vec Nat zero := nil
let c : Vec Nat (suc zero) := cons zero nil
