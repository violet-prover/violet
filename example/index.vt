import nat
import list
import vec
import equality
import compute

# whatever
let id {A : U} (x : A) : A := x
let id2 {B : U} (x : B) : B := id x

let z : Nat := zero
let one : Nat := suc zero
let s : Nat -> Nat := suc
